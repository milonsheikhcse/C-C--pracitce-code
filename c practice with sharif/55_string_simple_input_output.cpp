#include<bits/stdc++.h>
using namespace std;

int main() {
//string s;
    char s[100];
    ///gets(s); dia
//gets(s);
    //while(cin >> s)
    while(gets(s)) {
        if(s[0]=='\0') {
            return 0;
        }
        //puts(s);
        cout << s<< endl;

    }
    /**
            while(1) {
                gets(st);
                if(st[0] == '\0')
                    break;

                printf("%s\n",st);
            }
        or
            while(scanf("%s",st) != EOF){ // akane space er por nibe na..

                printf("%s\n",st);
            }
    /// ata right nicher ta

            while(gets(st)) {
            if(st[0] == '\0')
                break;
            printf("%s\n",st);
        }
        */


    return 0;
}
